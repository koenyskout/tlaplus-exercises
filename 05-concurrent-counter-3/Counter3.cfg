SPECIFICATION 
    Spec

CONSTANT 
    UNDEFINED = UNDEFINED \* Model value for an undefined value
    Threads = {t1, t2, t3, t4} \* Four threads (model values)

\* Won't check deadlock, because we expect to reach a final state
CHECK_DEADLOCK FALSE

INVARIANTS 
    TypeOK
    CounterOK
    NotDoneAndZero
    NotEndWithOne \* This property will fail

PROPERTIES 
    CounterN \* This property will fail